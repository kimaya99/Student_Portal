<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kimayadesai.portal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:theme="@style/Theme.AppCompat.Light"
        android:name=".CertificateApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
 >
        <activity
            android:name=".RegistrationLoginDeleteUser"
            android:windowSoftInputMode="stateVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DashboardActivity" />

        <activity android:name=".RegWaitListCourse"
            android:theme="@style/AppTheme"/>
        <activity android:name=".DropClassActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".DropWaitListActivity" android:theme="@style/AppTheme" />

        <activity
            android:name=".SelectFilteredSubject"
            android:label="@string/title_activity_base"
            android:theme="@style/AppTheme" />

    </application>

</manifest>